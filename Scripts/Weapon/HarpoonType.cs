using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//grab stuff from the save system we make and just set harpoon stats with switch statement on runtime
public enum HarpoonType {
    Standard,
    Serrated,
    Bill,

}
